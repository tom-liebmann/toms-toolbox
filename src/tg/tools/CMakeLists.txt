CMAKE_MINIMUM_REQUIRED( VERSION 2.6 )

PROJECT( tgTools )

# find source files
FILE(
    GLOB_RECURSE
    SOURCE_FILES
    ABSOLUTE ${CMAKE_CURRENT_SOURCE_DIR}
    *.cpp
)

ADD_LIBRARY(
    tgTools
    STATIC ${SOURCE_FILES}
)
