CMAKE_MINIMUM_REQUIRED( VERSION 2.6 )

PROJECT( ttbTools )

# find source files
FILE(
    GLOB_RECURSE
    SOURCE_FILES
    ABSOLUTE ${CMAKE_CURRENT_SOURCE_DIR}
    *.cpp
)

ADD_LIBRARY(
    ttbTools
    STATIC ${SOURCE_FILES}
)
