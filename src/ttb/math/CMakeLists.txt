CMAKE_MINIMUM_REQUIRED( VERSION 2.6 )

PROJECT( ttbMath )

# find source files
FILE(
    GLOB_RECURSE
    SOURCE_FILES
    ABSOLUTE ${CMAKE_CURRENT_SOURCE_DIR}
    *.cpp
)

ADD_LIBRARY(
    ttbMath
    STATIC ${SOURCE_FILES}
)

TARGET_COMPILE_OPTIONS(
    ttbMath
    PRIVATE -std=gnu++14
)
